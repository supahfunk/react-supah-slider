const style = () => {
  const output = {
    root: {
      position: 'relative',
      overflow: 'hidden',
    },
    slider: {
      position: 'absolute',
      top: 0,
      left: 0,
      display: 'flex',
    },
  }

  return output
}

export default style
